{
  "buildCommand": "pnpm --filter @ai-job-analyzer/shared build && pnpm --filter @ai-job-analyzer/frontend build",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
